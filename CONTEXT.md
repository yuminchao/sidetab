# SideTab Lite 标签视图

本上下文描述侧边栏标签与标签分组的核心界面概念，用于统一需求、设计文档和代码讨论中的术语。

## 语言

**分组块**：
一个标签分组在侧边栏中的完整视觉单元；展开时包含分组标题与成员标签，折叠时仅包含分组标题。
_避免使用_：分组 tab、分组行、分组卡片

**活动标签**：
当前 Chrome 窗口中被选中、可以接收页面操作的标签页；它可以是普通标签、固定标签或分组成员。
_避免使用_：选中 tab、当前分组

**标签树**：
由 Chrome 明确提供的打开来源关系组成的标签层级；父子关系只在双方均未分组或处于同一原生标签组时显示，不跨越原生标签组边界。
_避免使用_：树形分组、嵌套标签组

**父标签**：
Chrome 通过 `openerTabId` 明确标识为某个新标签打开来源的标签；侧边栏新增标签按钮在“作为当前标签的子标签”模式下使用操作时的活动标签作为父标签。
_避免使用_：父节点、上级分组

**子标签**：
拥有有效父标签且满足同一原生标签组边界的标签；子标签可以继续作为其他标签的父标签，形成多级标签树。
_避免使用_：子节点、组内标签

**根标签**：
当前标签树中没有有效父标签的标签；全局快捷网站、收藏夹和历史记录入口打开的标签默认作为根标签。
_避免使用_：无父节点标签、顶级分组

**标签子树**：
一个父标签及其全部直接和间接子标签组成的连续视觉单元；父标签折叠时仅隐藏后代，不关闭标签，活动标签所在的祖先路径始终展开。
_避免使用_：子分组、嵌套分组块

**解除父子关系**：
用户将子标签拖出标签子树后，该标签在当前标签树中提升为根标签，其后代关系保持不变。
_避免使用_：移出分组、取消嵌套

**兄弟标签**：
拥有同一父标签的直接子标签；兄弟标签按 Chrome 标签索引排序，可在同一父标签的直接子标签区域内调整顺序而不解除父子关系。
_避免使用_：同级分组、并列节点
