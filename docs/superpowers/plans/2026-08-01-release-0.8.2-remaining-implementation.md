# SideTab Lite 0.8.2 剩余发布工作实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在已完成收藏夹与历史记录合并搜索的基础上，完成快捷网站默认开启、恢复文本加号按钮、清理 `add-tab.svg`、升级版本并生成可安装的 0.8.2 Chrome 扩展包。

**Architecture:** 继续复用现有快捷设置模型、侧边栏渲染器和确定性发布脚本。默认值与旧设置迁移仅在 `shortcut-model` 中处理；按钮只调整静态标记和 CSS；发布阶段同步版本、资源白名单、中文文档和 ZIP 审计，不增加运行时监听器、缓存或网络行为。

**Tech Stack:** Chrome Extension Manifest V3、TypeScript、原生 DOM/CSS、Vitest、esbuild、PowerShell、Node.js 发布脚本。

---

## 范围约束

- 收藏夹与历史记录合并搜索已经完成，本计划只做其余 0.8.2 工作及最终发布。
- `bookmarks` 继续作为必需权限；不得新增主机权限、content script、可选权限或远程代码。
- 不修改快捷网站编辑、排序、favicon 缓存和最多 12 项限制。
- 不修改新增标签页的 Chrome API 调用、滚动位置、请求去重或错误反馈。
- 不增加轮询、收藏夹监听器、长期结果缓存或持久化搜索结果。

### Task 1：快捷网站默认开启与旧设置迁移

**Files:**
- Modify: `src/sidepanel/shortcut-model.ts`
- Modify: `tests/shortcut-model.test.ts`
- Modify: `tests/shortcut-store.test.ts`
- Modify: `tests/sidebar.test.ts`

- [ ] **Step 1：先写默认值和迁移回归测试**

  覆盖以下行为：新安装默认 `enabled: true`；旧对象缺少 `enabled` 时迁移为 `true`；显式 `enabled: false` 原样保留；字段存在但非布尔值时回退完整默认设置；恢复默认后开启并包含 OpenAI、Google、GitHub；侧边栏无存储设置时立即展示三个默认入口。

- [ ] **Step 2：运行专项测试并确认红灯原因**

  Run: `npm test -- --run tests/shortcut-model.test.ts tests/shortcut-store.test.ts tests/sidebar.test.ts`

  Expected: 仅因当前默认值为 `false`、缺少 `enabled` 仍被判为无效或默认入口被隐藏而失败。

- [ ] **Step 3：实现最小迁移逻辑**

  将 `createDefaultShortcutSettings()` 的 `enabled` 改为 `true`。`validateShortcutSettings()` 只在对象没有自有 `enabled` 字段时使用 `true`；如果字段存在则仍要求布尔值，保证已保存的 `false` 不被覆盖，非法值仍触发完整默认回退。

- [ ] **Step 4：运行专项、类型和差异检查**

  Run: `npm test -- --run tests/shortcut-model.test.ts tests/shortcut-store.test.ts tests/sidebar.test.ts`

  Run: `npm run typecheck`

  Run: `git diff --check`

- [ ] **Step 5：提交默认入口改动**

  提交前读取 `C:\Users\NUC\.codex\rules\git-commit.md`，仅暂存本任务文件并按规则提交。

### Task 2：恢复文本加号按钮并移除 SVG 资源

**Files:**
- Modify: `src/sidepanel/index.html`
- Modify: `src/sidepanel/sidebar.css`
- Delete: `assets/icons/add-tab.svg`
- Modify: `scripts/release-files.mjs`
- Modify: `scripts/check-dist.mjs`
- Modify: `tests/sidepanel-markup.test.ts`
- Modify: `tests/sidepanel-css.test.ts`
- Modify: `tests/assets.test.ts`
- Modify: `tests/release-scripts.test.ts`

- [ ] **Step 1：先写按钮标记、样式和资源红灯测试**

  标记测试要求按钮只包含 `+` 文本、无 SVG 子元素且保留标题和 `aria-label`。CSS 测试要求 `44px × 24px`、`margin: 3px auto`、`border: 1px solid ButtonBorder`、`border-radius: 5px`、`font: inherit`、`font-size: 20px`、`line-height: 1`、网格居中，并确认不存在 `.new-tab-button::before` 和 `add-tab.svg` 引用。资源与发布测试要求源码、`dist` 和 ZIP 白名单不包含 `add-tab.svg`，审核文件数为 14。

- [ ] **Step 2：运行专项测试并确认红灯原因**

  Run: `npm test -- --run tests/sidepanel-markup.test.ts tests/sidepanel-css.test.ts tests/assets.test.ts tests/release-scripts.test.ts`

  Expected: 当前按钮为空、CSS 使用 SVG mask、资源仍存在且发布白名单仍为 15 项，因此新增断言失败。

- [ ] **Step 3：实现文本按钮并清理资源契约**

  在 `#new-tab-button` 内写入单个 `+` 字符；恢复批准的边框和字体样式；删除伪元素规则和 SVG 文件；从发布白名单、SVG 路径数量校验及测试 fixture 中移除该资源。构建仍复制 `assets/icons` 目录，不增加单独资源分支。

- [ ] **Step 4：运行专项、构建和差异检查**

  Run: `npm test -- --run tests/sidepanel-markup.test.ts tests/sidepanel-css.test.ts tests/assets.test.ts tests/release-scripts.test.ts`

  Run: `npm run build`

  Run: `node scripts/check-dist.mjs`

  Run: `git diff --check`

  Expected: `dist` 精确包含 14 个审核文件，且任何位置均无 `add-tab.svg`。

- [ ] **Step 5：提交按钮和资源改动**

  提交前读取 Git 提交规则，仅暂存本任务文件并提交。

### Task 3：升级 0.8.2 并同步中文发布资料

**Files:**
- Modify: `manifest.json`
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `README.md`
- Modify: `docs/privacy-policy.md`
- Modify: `docs/chrome-web-store-checklist.md`
- Modify: `update.log`
- Modify: `tests/smoke.test.ts`
- Modify: `tests/release-scripts.test.ts`

- [ ] **Step 1：先写版本、文档和 14 文件发布断言**

  测试要求四处根版本统一为 `0.8.2`，README 指向 `release/sidetab-lite-0.8.2.zip`，发布产物精确 14 项，更新日志含收藏夹合并搜索、快捷网站默认开启、文本加号按钮和性能边界。三份中文发布资料不得继续宣称快捷入口默认关闭、按钮使用新增标签页 SVG 或产物包含 15 项。

- [ ] **Step 2：运行 smoke 与发布测试并确认红灯**

  Run: `npm test -- --run tests/smoke.test.ts tests/release-scripts.test.ts`

  Expected: 当前版本仍为 0.8.1，README、清单和更新日志仍描述旧发布状态。

- [ ] **Step 3：同步版本和中文资料**

  使用 `npm version 0.8.2 --no-git-tag-version` 更新 npm 元数据，再把 Manifest 升级为 0.8.2。README、隐私政策、商店检查清单和 `update.log` 使用中文准确说明最终行为、必需 `bookmarks` 权限、新权限升级提示、14 文件产物、默认入口迁移及文本加号按钮。保留旧版本更新记录。

- [ ] **Step 4：运行完整发布前检查**

  Run: `npm run check`

  Run: `npm run benchmark:tabs`

  Run: `git diff --check`

  Expected: 类型检查、全部测试、构建、14 文件 dist 审计和基准均成功；工作树只包含本任务预期文件。

- [ ] **Step 5：提交 0.8.2 发布元数据**

  提交前读取 Git 提交规则，仅暂存本任务文件并提交。

### Task 4：生成安装包并完成最终验收

**Files:**
- Generated (ignored): `dist/**`
- Generated (ignored): `release/sidetab-lite-0.8.2.zip`

- [ ] **Step 1：生成确定性安装包**

  Run: `npm run package`

  Expected: 生成 `release/sidetab-lite-0.8.2.zip`，不会删除 `release` 中其他版本文件。

- [ ] **Step 2：审计 ZIP 内容与版本**

  使用 PowerShell `System.IO.Compression.ZipFile` 读取 ZIP，断言恰好 14 个条目、条目集合与 `scripts/release-files.mjs` 一致、包内 Manifest 为 0.8.2、无 `add-tab.svg`、无快捷网站兼容 PNG、无 source map、无额外权限或远程代码。

- [ ] **Step 3：验证确定性和哈希**

  连续执行两次打包并比较 SHA-256，确认内容一致；记录最终 ZIP 路径、字节数和哈希。

- [ ] **Step 4：浏览器视觉与交互验收**

  在可用的 Chrome 扩展环境中加载 `dist`；若普通 localhost 无法提供真实扩展 API，则只做静态布局预览并明确限制。检查 180px、240px 和常规宽度：快捷入口默认可见；加号在 44×24 按钮内上下左右居中；按钮随标签列表滚动；底部搜索与设置固定；无横向滚动、重叠或布局跳动。

- [ ] **Step 5：最终独立审查和工作树检查**

  Run: `npm run check`

  Run: `npm run benchmark:tabs`

  Run: `git diff --check`

  Run: `git status --short`

  Expected: 所有受控源文件已提交、工作树干净，最终代码审查无 Critical 或 Important 问题；仅保留被 `.gitignore` 忽略的 `dist` 和发布 ZIP。

