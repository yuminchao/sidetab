# 领域文档

本仓库采用单上下文布局。

## 探索代码前

- 如果根目录存在 `CONTEXT.md`，先读取它。
- 读取 `docs/adr/` 中与当前工作范围相关的 ADR。
- 文件不存在时静默继续，不要求预先创建。
- `CONTEXT.md` 和 ADR 由领域建模流程在术语或决策确定后按需创建。

## 术语

输出中的领域概念应使用 `CONTEXT.md` 定义的术语，避免使用其明确排除的同义词。遇到尚未定义但确有必要的概念时，记录为领域建模缺口。

## ADR 冲突

如果方案与现有 ADR 冲突，必须明确指出冲突及原因，不得静默覆盖已有决策。
